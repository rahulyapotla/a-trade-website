<%- include('../partials/header.ejs')%>
<body>
  <a href='/trades/available_breeds'>Available Breeds</a>
</div>
</nav>
   <div class = "content">
    <div class="row">
      <div class="d-flex justify-content-center col-3">Name:</div>
      <div class="col-9"><%= item.name%></div>
    </div>
    <div class="row">
      <div class="d-flex justify-content-center col-3">Category:</div>
      <div class="col-9"><%= item.category%></div>
    </div>
    <div class="row">
      <div class="d-flex justify-content-center col-3">Details:</div>
      <div class="col-9"><%= item.details%></div>
    </div>
    <div class="row">
      <div class="d-flex justify-content-center col-3">Status:</div>
      <div class="col-9"><%= item.status%></div>
    </div>
      <div class="d-flex mt-2 justify-content-center"><img class="img-size" src="<%= item.image %>" alt="Logo"></div>
    </div>
    <form class="d-flex justify-content-around">
      <button class="btn btn-primary" type="submit" formmethod="GET" formaction="/trades/available_breeds/<%= item.id%>/edit">Edit</button>
      <button class="btn btn-danger" type="submit" formmethod="POST" formaction="/trades/available_breeds/<%= item.id%>?_method=DELETE">Delete</button>
    </form>
<%- include('../partials/footer.ejs')%>
